mixin social(clase,link,target)
  a.social_link(class=""+clase href=""+link target=""+target)
mixin article0(nombre,fecha,imagen,descipcion,conocimientos)
  article.project
    .project_details
      h3.project_title=nombre
      h6.project_course
      p.project_date
        small
          strong Fecha: 
          =fecha
      p.project_desciption=descipcion
      p.project_desciption=conocimientos
    figure.project_imageContainer
      img.project_image(src=""+imagen)
mixin article1(nombre,fecha,imagen,descipcion,conocimientos,link)
  article.project
    .project_details
      h3.project_title=nombre
      h6.project_course
      p.project_date
        small
          strong Fecha: 
          =fecha
      p.project_desciption=descipcion
      p.project_desciption=conocimientos
      a(href=""+link): button.project_btn Ver proyecto
    figure.project_imageContainer
      img.project_image(src=""+imagen)
mixin article2(nombre,fecha,imagen,descipcion,conocimientos,link1,link2,text1,text2)
  article.project
    .project_details
      h3.project_title=nombre
      h6.project_course
      p.project_date
        small
          strong Fecha: 
          =fecha
      p.project_desciption=descipcion
      p.project_desciption=conocimientos
      .grid2btns
        a(href=""+link1): button.project_btn=text1
        a(href=""+link2): button.project_btn=text2
    figure.project_imageContainer
      img.project_image(src=""+imagen)
mixin badge(icon)
  img(src="/images/badges/"+icon)